export function notificationIndicator() {
    return Widget.Box({
        children: [
            Widget.Icon({
                icon: "preferences-system-notifications-symbolic",
            }),
        ]
    })
}
